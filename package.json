{
  "private": true,
  "scripts": {
    "pretest": "npm --prefix server install --include=dev --no-fund --no-audit",
    "test": "npm --prefix server test",
    "test:unit": "npm --prefix server run test:unit",
    "test:integration": "npm --prefix server run test:integration",
    "test:system": "npm --prefix server run test:system"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^2.2.2",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mysql2": "^3.17.1",
    "uuid": "^13.0.0"
  }
}
